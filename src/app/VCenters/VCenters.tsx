import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const VCenters: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">Manage Vaccination Centers</Title>
  </PageSection>
)

export { VCenters };
