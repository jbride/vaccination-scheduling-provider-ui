import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Appointments: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">Appointments Page Title</Title>
  </PageSection>
)

export { Appointments };
